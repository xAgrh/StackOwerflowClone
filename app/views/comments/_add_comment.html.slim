.comment-errors 

= form_for [@question, @comment], remote: true, data: { type: :json } do |f| 
  p  
    = f.label :body
    = f.text_area :body
  
  p
    = f.submit 'Добавить комментарий', class: "btn btn-info"